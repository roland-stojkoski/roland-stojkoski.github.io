# Planned work & tracked compromises

Two sections live here:

1. **Backlog** — manually curated, ordered roughly by priority. Edit freely.
2. **Codetags found in the codebase** — auto-generated from `TODO`/`FIXME`/
   `HACK`/`XXX`/`NOTE` comments by `scripts/generate-planned.mjs`. Never edit
   it by hand; it is overwritten on every build and commit.

## Backlog

- [ ] Enable GitHub Discussions + install the giscus app, then fill
      `repoId`/`categoryId` in `src/lib/config.ts` to activate comments
- [ ] Backfill timeline events for 2023–2026 (the gap between the two
      articles)
- [ ] Replace the YouTube macro demo video in the modernization article with
      real content
- [ ] Optimize/resize the large photos in `static/` (avatar ships at full
      resolution)
- [ ] Publish Storybook somewhere (GitHub Pages subpath or Chromatic)
- [ ] Add a Lighthouse/perf budget check to CI
- [ ] Article tags + search once there are enough articles to warrant it
- [ ] Turn the repo into a reusable template for others (carried over from
      the 2023 TODO list)
- [ ] Look into symlinking the duplicated agent steering instead of copying it
      with `scripts/sync-agents.mjs`. Every tracked file under `.claude/` and
      `.agents/` is a byte-identical pair today: `rules/commits.md`,
      `rules/comments.md`, `rules/gemini.md` and the six skills
      (`add-macro`, `add-timeline-event`, `sync-agents`, `validate-styles`,
      `verify-site`, `write-article`) — only `.claude/settings.json` and the
      gitignored `*.local.json` files are per-tool. Weigh two real constraints
      before switching: git stores symlinks fine, but a Windows checkout
      without developer mode (or with `core.symlinks=false`) materialises them
      as plain text files containing the target path, which would hand agents
      garbage instead of rules; and it is unverified whether Claude Code,
      Gemini and Antigravity follow symlinks when discovering rules and skills
      — test both before committing to it. If either blocks it, keep the
      copies and say so here.
- [ ] Delete the `rules/gemini.md` pass-through from both trees — it only
      re-imports the root `GEMINI.md`, whose content `CLAUDE.md` and
      `AGENTS.md` already reach, so it adds a second path to the same text

<!-- codetag-scan:start (everything below is overwritten) -->

## Codetags found in the codebase

Regenerated by `npm run planned` (also runs on every build and commit).

### TODO

- `.agents/rules/comments.md:10` — ` planned improvement, not blocking
- `src/lib/config.ts:23` — enable GitHub Discussions on the repo, install the giscus app, then

### FIXME

- `.agents/rules/comments.md:11` — ` known defect or fragile behavior being tolerated

### HACK

- `.agents/rules/comments.md:12` — ` deliberate ugliness that works around something external
- `src/lib/data/timeline.ts:16` — year-only events get a mid-year date so they sort sensibly
- `src/routes/attributions/+page.svelte:4` — the notices are served from static/THIRD-PARTY-NOTICES.md, a

### XXX

- `.agents/rules/comments.md:13` — ` danger zone — do not touch without understanding

### NOTE

- `.agents/rules/comments.md:14` — ` non-obvious fact the next reader needs
- `src/lib/components/AccessibilityMenu.svelte:329` — the panel stays mounted so its open/close transition can run;
- `src/lib/components/Hero.svelte:74` — keep this output line uncolored — theme text classes are unreadable on the always-dark console in the light theme

<!-- codetag-scan:end -->
